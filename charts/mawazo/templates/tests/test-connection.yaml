apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mawazo.fullname" . }}-test-connection"
  labels:
    {{- include "mawazo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mawazo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
